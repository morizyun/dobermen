%h1 Listing projects

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Name
      %th Last Activity
      %th Ruby Version
      %th Rails Version
      %th Message
      %th{ colspan: 2 } Action

  %tbody
    - @projects.each do |project|
      = cell(:project, project).(:table_tr)
